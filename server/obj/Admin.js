class Admin {
    /** Constructor,
     *  TODO: add more description.
     * 
     * @param {The client Id of the admin} clientId
     */
    constructor(clientId) {
        this.clientId = clientId;
    }
}

module.exports = Admin;