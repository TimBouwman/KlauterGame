class Output {
    /** Constructor,
     *  TODO: add more description.
     * 
     * @param {The client Id of the output device} clientId
     */
    constructor(clientId) {
        this.clientId = clientId;
    }
}

module.exports = Output;